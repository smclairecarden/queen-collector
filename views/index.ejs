<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <div class="welcome-message">
  <h1>Hello, <%= user ? user.profile.name : "Drag Race Fan!" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } else { %>
    <h2>Welcome to the Queen Collector, a place for Drag Race fans to add and share their favorite queens. Log in and have fun! Racers, start your engines!</h2>
  <% } %> 
  </div>
</main>

<%- include('./partials/footer') %>
